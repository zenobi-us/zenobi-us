name: Auto Approve Dependabot PRs

on:
  pull_request_target:
    types: [labeled]
  pull_request:
    types: [labeled]

jobs:
  AutoUpdateDependabot:
    name: Auto-Approve and enable Auto-Merge for all Dependabot PRs
    runs-on: ubuntu-latest
    if: github.event.pull_request.user.login == 'dependabot[bot]' && contains(github.event.pull_request.labels.*.name, 'dependencies')
    steps:
      # Enable auto-merge *before* issuing an approval.
      - name: Enable Github Automerge
        uses: alexwilson/enable-github-automerge-action@415954b071d8c4ed26c6875e781ac730712ac09c
        with:
          github-token: '${{ secrets.ZENOBIUS_MONOREPO_RELEASE_PLEASE_TOKEN }}'

      # We "trust" dependabot updates (this still requires all other checks to pass!)
      - uses: hmarr/auto-approve-action@8f929096a962e83ccdfa8afcf855f39f12d4dac7
        with:
          github-token: '${{ secrets.ZENOBIUS_MONOREPO_RELEASE_PLEASE_TOKEN }}'
